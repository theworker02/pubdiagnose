# FAQ

**Does PubDoctor replace `dart pub get`?** No.

**Will `fix` edit my files by default?** No — propose only unless `--apply`.

**Is Flutter required?** No. Flutter SDK analysis uses package metadata constraints.

**Can I ignore a rule?** Yes — `pubdoctor.yaml` `ignore` / `severities`.
