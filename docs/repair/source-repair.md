# Source repair

Source repairs require explicit permission and unique targets (broken import with
exactly one candidate, stale `part`, official analyzer fixes).

Ambiguous cases emit explain-only guidance (no auto rewrite).
