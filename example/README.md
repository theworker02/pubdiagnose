# PubDiagnose example

Run from the package root after `dart pub get`:

```bash
dart run example/pubdiagnose_example.dart
```

The example opens the current project with `PubDoctor.open`, runs `check()`,
and prints a short status summary.
